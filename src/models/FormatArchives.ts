export default class FormatArchives {
    photos: {
        uri?: string;
        type?: string;
        name?: string;
      }[] = [];
      videos: {
        uri?: string;
        type?: string;
        name?: string;
      }[] = [];
}
